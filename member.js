import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.4/firebase-app.js";import { getAuth, signInWithEmailAndPassword, createUserWithEmailAndPassword, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/10.12.4/firebase-auth.js";import { getFirestore, collection, addDoc, getDocs, query, where, orderBy, serverTimestamp, doc, getDoc, setDoc } from "https://www.gstatic.com/firebasejs/10.12.4/firebase-firestore.js";import { getStorage, ref, uploadBytes, getDownloadURL } from "https://www.gstatic.com/firebasejs/10.12.4/firebase-storage.js";import { firebaseConfig } from "./firebase-config.js";let app,auth,db,storage,currentUser;if(firebaseConfig.apiKey.includes("PUT_")){loginMsg.textContent="لم يتم ربط Firebase بعد."}else{app=initializeApp(firebaseConfig);auth=getAuth(app);db=getFirestore(app);storage=getStorage(app);onAuthStateChanged(auth,async u=>{currentUser=u;loginBox.classList.toggle("hidden",!!u);logoutBtn.classList.toggle("hidden",!u);if(!u)return;const r=doc(db,"members",u.uid);let s=await getDoc(r);if(!s.exists())await setDoc(r,{email:u.email,uid:u.uid,blocked:false,createdAt:serverTimestamp()});s=await getDoc(r);if(s.data().blocked){blockedBox.classList.remove("hidden");return}memberBox.classList.remove("hidden");myBox.classList.remove("hidden");const snap=await getDocs(query(collection(db,"archive_items"),where("createdBy","==",u.uid),orderBy("createdAt","desc")));myStatus.textContent=`${snap.size} إضافة`;myList.innerHTML=snap.docs.map(d=>{const x=d.data();return `<div class="adminRow"><div><b>${x.title}</b><br><span class="badge">${x.status}</span></div></div>`}).join("")});loginForm.onsubmit=async e=>{e.preventDefault();try{await signInWithEmailAndPassword(auth,email.value,password.value)}catch(err){loginMsg.textContent="تعذر الدخول"}};registerBtn.onclick=async()=>{try{await createUserWithEmailAndPassword(auth,email.value,password.value)}catch(err){loginMsg.textContent=err.message}};logoutBtn.onclick=()=>signOut(auth);contentForm.onsubmit=async e=>{e.preventDefault();let fileUrl="",fileType="";const f=file.files[0];if(f){fileType=f.type;const fileRef=ref(storage,`pending/${currentUser.uid}/${Date.now()}-${f.name}`);await uploadBytes(fileRef,f);fileUrl=await getDownloadURL(fileRef)}await addDoc(collection(db,"archive_items"),{title:title.value,date:date.value,location:location.value,region:region.value,type:type.value,desc:desc.value,fileUrl,fileType,status:"pending",createdBy:currentUser.uid,createdByEmail:currentUser.email,createdAt:serverTimestamp()});saveMsg.textContent="تم الإرسال للمراجعة";contentForm.reset()}}